import QtQuick

import SVSCraft.UIComponents

Rectangle {
    border.color: Theme.foregroundPrimaryColor
    border.width: 1
    color: Qt.rgba(Theme.foregroundPrimaryColor.r, Theme.foregroundPrimaryColor.g, Theme.foregroundPrimaryColor.b, Theme.foregroundPrimaryColor.a * 0.375)
}
